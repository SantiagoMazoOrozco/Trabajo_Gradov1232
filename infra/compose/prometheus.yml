global:
  scrape_interval: 1s
  evaluation_interval: 1s

scrape_configs:
- job_name: 'app'
  file_sd_configs:
  - files:
    - /var/opt/state/prom_targets.json
    refresh_interval: 1s

- job_name: 'node_exporter'
  static_configs:
  - targets: [ 'node-exporter:9100' ]
    labels:
      job: node
